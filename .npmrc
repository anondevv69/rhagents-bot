# @privy-io/react-auth pins an older `ox` via optional `permissionless` than our viem —
# harmless (we don't use account abstraction), but strict peer resolution fails CI installs.
legacy-peer-deps=true
